---
import "../styles/global.css";
---
<body>
    <div class="newsletter">
        <div class="container-new">
            <h1>Subscribe Our Newsletter</h1>
            <h2>Build stronger Customer Relationships with Consultalk</h2>
        </div>
        <div class="container-new-1">
    <p>your mail address</p>
    <button class="sub">
        Subscribe
    </button>
        </div> 
    </div>
    
    </body>
    <style>  
body{
    background-color: var(--Primary-03);
    display: grid;
    grid-template-rows: auto 1fr auto; 
    grid-template-columns:
        [full-start] 1fr [content-start] minmax(0, 0.65ch) [content-end] 1fr [full-end];
    gap: 1rem; 
}

    .newsletter{
border-radius: 10px;
font-size: var(--step--1);
background-color: var(--Primary-03);
width: calc(100% - 2rem);
padding: 3%;
grid-column: full;
  
  > * {
    grid-column: content;
  }

  .container-new-1{
    font-size: var(--step-0);
     display: grid;
    grid-columns: 2fr 1fr;
        }

        .container-new{

        }
        .sub{
background-color: var(--Primary-01);
border-radius: 10px;
font-size: var(--step--1);
}

}

.full-bleed {
  grid-column: full;
  display: grid;
  grid-template-columns: subgrid;
  
  > * {
    grid-column: content;
  }
}

* {
  box-sizing: border-box;
}

</style>